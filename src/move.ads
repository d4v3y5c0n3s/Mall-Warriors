package Move is

  --type Move_Step
  -- a record type that says what happens from frame X to frame X during the course of performing this move
  
  type Move is tagged null record;
    --input_sequence
    --[list/array of Move_Step]
  --end record;

end Move;
